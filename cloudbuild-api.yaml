steps:
  - name: 'gcr.io/cloud-builders/docker'
    args: 
      - 'build'
      - '--no-cache'
      - '-t'
      - 'gcr.io/$PROJECT_ID/inka-api:latest'
      - '-f'
      - 'apps/api/Dockerfile'
      - '.'

images:
  - 'gcr.io/$PROJECT_ID/inka-api:latest'

options:
  machineType: 'N1_HIGHCPU_8'
